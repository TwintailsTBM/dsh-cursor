# Verification overlay: disable BOTH plugin rows, so a real boot proves the
# rows are switchable off.
- id: cursor-rules
  config:
    enabled: false
- id: cursor-skills
  config:
    enabled: false
