# Verification overlay: isolate the source patch and disable BOTH plugin rows, so
# a real boot proves the rows are switchable off.
- id: agent-instructions
  config:
    maxBytes: 65536
    cursorRules:
      enabled: false
- id: skill-cursor
  disabled: true
- id: cursor-rules
  config:
    enabled: false
- id: cursor-skills
  config:
    enabled: false
