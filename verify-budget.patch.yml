# Verification overlay: shrink the rules budget so the truncation path runs
# against real Cursor content.
- id: cursor-rules
  config:
    enabled: true
    maxBytes: 3000
    includeUserRules: true
- id: cursor-skills
  config:
    enabled: true
